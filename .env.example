# Optional: enables live Claude responses for agents and simulations
ANTHROPIC_API_KEY=
# Optional: override default model (spec default: claude-sonnet-4-20250514)
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Optional: Neon Postgres — governance audit log persistence (table created on first write)
DATABASE_URL=
